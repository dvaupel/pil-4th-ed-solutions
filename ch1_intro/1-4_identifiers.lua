___ = nil       -- valid
_end = nil      -- valid
End = nil       -- valid
--end = nil     -- reserved
--until? = nil  -- until is reserved, question mark is an illegal character
--nil = nil     -- reserved
NULL=nil        -- valid
--one-step=nil  -- dash is invalid (only [_a-zA-Z0-9] is allowed!)

-- Uncomment lines with invalid identifiers to see the error message.